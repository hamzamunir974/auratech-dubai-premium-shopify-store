<section class="main-page section-padding">
  <div class="page-width">
    <div class="page-panel">
      <p class="section-kicker">{{ shop.name }}</p>
      <h1>{{ page.title }}</h1>
      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main page"
}
{% endschema %}
